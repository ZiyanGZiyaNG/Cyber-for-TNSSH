const textarea = document.getElementById("msg")
const button = document.getElementById("send")
const message = document.getElementById("message")

button.addEventListener("click", function()
{
    const py = textarea.value
});